/**
 * Geo Utilities
 * 
 * Simple Mercator projection and coordinate transformation helpers.
 * 
 * Features to implement:
 * - latLngToMeters(lat, lng): convert to meters (x, y) in Web Mercator
 * - metersToLatLng(x, y): reverse conversion
 * - getBearing(lat1, lng1, lat2, lng2): calculate compass bearing (0-360°)
 * - getDestinationPoint(lat, lng, distance, bearing): project point from origin
 * - normalizeHeading(heading): ensure 0-360° range
 * 
 * Useful for:
 * - Converting GPS deltas to local meters for AR placement
 * - Calculating which direction characters face
 * - Map projections and coordinate systems
 */

// TODO: Implement Mercator projection and bearing calculations
