/**
 * Distance Utilities
 * 
 * Calculates distances between GPS coordinates using the Haversine formula.
 * 
 * Features to implement:
 * - haversineDistance(lat1, lng1, lat2, lng2): returns distance in meters
 * - isWithinRadius(userPos, targetPos, radiusMeters): boolean check
 * - getClosestSpawn(userPos, spawns): finds nearest spawn point
 * - sortByDistance(userPos, locations): sorts locations by proximity
 * 
 * Haversine formula accounts for Earth's curvature and provides accurate
 * distance calculations for AR interaction ranges (typically < 100m).
 */

// TODO: Implement Haversine distance calculations and proximity checks
